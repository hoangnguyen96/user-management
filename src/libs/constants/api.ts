const API_NAME = "user_management";

const ENDPOINTS = {
  USER: "user",
  PRODUCT: "product",
};

const OPTIONS = ["Edit", "Delete"];

const STALE_TIME_API = 1000 * 60 * 5;
const DELAY_TIME_API = 2;

export { API_NAME, ENDPOINTS, OPTIONS, STALE_TIME_API, DELAY_TIME_API };
